---
import ProjectCard from "./ProjectCard.astro";
import { projects } from "../data/projects.js";
import { SOCIAL_LINKS } from "../config.js";
---

<section id="projects" class="py-8 lg:py-16 px-4 sm:px-6 lg:px-24">
  <!-- Profile Github -->
  <div class="flex flex-col items-center gap-6 mb-12 text-center">
    <img
      src="https://github.com/danielvflores.png"
      alt="Foto de GitHub"
      class="w-32 h-32 sm:w-40 sm:h-40 rounded-full object-cover shadow-lg"
    />
    <div class="max-w-2xl">
      <h2 class="text-2xl sm:text-3xl font-bold mb-3">
        Hola, estás en el apartado de mis proyectos 👋
      </h2>
      <p class="text-gray-600 text-sm sm:text-base leading-relaxed mb-4">
        Desarrollador backend especializado en Node.js, TypeScript y Java. Mi
        enfoque está en crear sistemas eficientes y bien estructurados. Como
        Trainee en constante crecimiento, cada proyecto es una oportunidad para
        aplicar mejores prácticas y arquitecturas sólidas que voy aprendiendo
        con el tiempo.
      </p>
      <a
        href={SOCIAL_LINKS.github}
        class="text-blue-600 hover:underline text-sm sm:text-base inline-block"
        target="_blank"
        rel="noopener noreferrer"
      >
        Ver perfil completo en GitHub →
      </a>
    </div>
  </div>

  <!-- Stats Section -->
  <div class="grid grid-cols-2 md:grid-cols-4 gap-4 md:gap-6 max-w-4xl mx-auto mb-12">
    <div class="bg-gradient-to-br from-blue-50 to-blue-100 rounded-xl p-4 md:p-6 text-center border border-blue-200 hover:shadow-xl hover:shadow-blue-200/60 hover:scale-105 hover:border-blue-300 transition-all duration-300 cursor-pointer">
      <div class="text-2xl md:text-3xl font-bold text-blue-700 mb-1 md:mb-2">{projects.length}</div>
      <div class="text-xs md:text-sm font-medium text-blue-600">Proyectos Completados</div>
    </div>
    <div class="bg-gradient-to-br from-blue-50 to-blue-100 rounded-xl p-4 md:p-6 text-center border border-blue-200 hover:shadow-xl hover:shadow-blue-200/60 hover:scale-105 hover:border-blue-300 transition-all duration-300 cursor-pointer">
      <div class="text-2xl md:text-3xl font-bold text-blue-700 mb-1 md:mb-2">8+</div>
      <div class="text-xs md:text-sm font-medium text-blue-600">Tecnologías Dominadas</div>
    </div>
    <div class="bg-gradient-to-br from-blue-50 to-blue-100 rounded-xl p-4 md:p-6 text-center border border-blue-200 hover:shadow-xl hover:shadow-blue-200/60 hover:scale-105 hover:border-blue-300 transition-all duration-300 cursor-pointer">
      <div class="text-2xl md:text-3xl font-bold text-blue-700 mb-1 md:mb-2">100%</div>
      <div class="text-xs md:text-sm font-medium text-blue-600">Código Open Source</div>
    </div>
    <div class="bg-gradient-to-br from-blue-50 to-blue-100 rounded-xl p-4 md:p-6 text-center border border-blue-200 hover:shadow-xl hover:shadow-blue-200/60 hover:scale-105 hover:border-blue-300 transition-all duration-300 cursor-pointer">
      <div class="text-2xl md:text-3xl font-bold text-blue-700 mb-1 md:mb-2">1+</div>
      <div class="text-xs md:text-sm font-medium text-blue-600">Años de Experiencia</div>
    </div>
  </div>

  <!-- Projects Grid -->
  <div class="grid gap-6 grid-cols-1 md:grid-cols-2 xl:grid-cols-3 max-w-6xl mx-auto justify-center">
    {projects.map(project => (
      <div class="flex justify-center">
        <ProjectCard
          title={project.title}
          description={project.description}
          techs={project.techs}
          repoUrl={project.repoUrl}
          demoUrl={project.demoUrl}
          featured={project.featured}
          status={project.status}
          type={project.type}
          icon={project.icon}
          gradient={project.gradient}
          year={project.year}
        />
      </div>
    ))}
  </div>
</section>
